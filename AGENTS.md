# AGENTS.md

## 目的

このリポジトリは、Linux 環境でスクリーンを即座に暗くするためのスクリプトを管理します。

## 開発方針

- 対象は Linux デスクトップ環境とし、利用する表示サーバーやデスクトップ環境への依存は README とスクリプト内に明記する。
- 画面を暗くする処理は、ユーザーが明示的に実行した場合に限定し、バックグラウンドで予期せず動作させない。
- 既存の画面設定を変更する場合は、復元方法を用意するか、変更内容を明確に説明する。
- シェルスクリプトは安全性と可読性を優先し、必要に応じて `shellcheck` で検査する。
- 外部コマンドの存在確認、エラー処理、終了ステータスを適切に扱う。
- 変更に関連する動作確認を行い、確認できない環境差異は記録する。

## ファイル構成

- 実行スクリプトは用途が分かる名前を付け、実行方法と依存関係を README に記載する。
- `README.md`: GitHub の既定表示と利用者向けの英語ドキュメント。
- `README_ja-JP.md`: 利用者向けの日本語ドキュメント。
- `dev-notes/session-YYYY-MM-DD.md`: セッションごとの内部開発記録、判断理由、検証結果、TODO。
- 一時ファイルや生成物、ローカル環境固有の設定はコミットしない。
- 秘密情報、ユーザー固有の絶対パス、不要な個人情報をリポジトリに含めない。

## ドキュメントの役割

- README には、利用者が必要とする目的、対応環境、使い方、制約だけを記載する。
- `dev-notes/` には、設計上の判断、試行錯誤、却下した案、コマンドの実行結果、引き継ぎ事項を記録する。
- ユーザー向けの仕様と内部の開発履歴を、同じ文書へ混在させない。

## セッション記録

- 作業開始時に `dev-notes/session-YYYY-MM-DD.md` を作成し、日付と作業範囲を記載する。
- 記録は生のログよりも、決定事項とその理由を優先する。
- 重要な検証は、必要に応じてコマンドと簡潔な出力を記録する。秘密情報や個人情報は記録しない。
- 作業終了時に、変更内容、コミット、未完了事項を短くまとめる。
- 設計判断は次の形式を基本とする。

  ````markdown
  - Topic: 判断対象
    - Decision: 最終的な決定
    - Rationale: 決定理由
    - Alternatives Considered: 検討した代替案（任意）
  ````

- コマンド記録が必要な場合は、次の形式を使用する。

  ````markdown
  - {時刻} Ran: `{command}`
    Output:
    ```text
    {簡潔な出力}
    ```
    Result: 結果の要約
  ````

## 変更時の確認

変更後は、可能な範囲で以下を実行する。

1. `shellcheck` による静的検査
2. `bash -n` による構文検査
3. 実際の表示環境での暗転・復元動作の確認

## コミットメッセージ

- コミットメッセージは Conventional Commits 形式（`type: description`）にする。
- `feat` は機能追加、`fix` は不具合修正、`docs` は文書変更、`refactor` は動作を変えない整理に使用する。
- メッセージは簡潔な命令形で書き、変更の目的が分かる内容にする。
